---
**Hello C# Syntax Contest!**
===
---

**注意点**  

以下の問ではxxをするプログラムを書けという形式で問題が出題されることがあるが、
そこで解答するプログラムは必要最小限で良い。例えば、「画面にHelloと出力するプログラム書け」という問題では、

    using System;
    class Program
    {
        static void Main()
        {
            Console.WriteLine("Hello");
        }
    }

と書くのはもちろん問題ないが、単に

    Console.WriteLine("Hello");

でも良い。要は回答者が理解していることが採点者に分かれば良い。

また、名前空間に関してはQ1を除いて省略して良い。



Q1. **Hello C#**
---

(1) C#はが主に動作するMicrosoftのランタイムの名前をかけ。:ヒント .N\*T Fr\*\*\*\*\*\*k (\*は伏せ字)

(2) プログラミング言語によって記述されたソースコードをコンピュータが実行できる形式(C#の場合は中間言語)に変換することを何というか。カタカナ5文字で答えよ。

(3)以下のプログラムの4行目を完全修飾名で書け。  

    using System;
    class Program
    {
        static void Main()
        {
            Console.WriteLine("Hello");
        }
    }

(4) Console.WriteLine();をVisualStudio上で書くときは「○○ Tab Tab」と入力するのが便利である。  
○○に入るアルファベット2文字を書け。

(5) Console.WriteLineメソッドとConsole.Writeメソッドの違いを答えよ。

(6) Console.WriteLineメソッドを以下のように記述した時何と出力されるか答えよ。

    Console.WriteLine("a={0}, b={1}", 10, 0.5);

(7) ユーザーから一行の入力を受け取るConsoleクラスのメソッドの名前をかけ。



Q2. **変数とデータ型**
---

(1) 型int, 変数名numの変数を宣言し、10で初期化する一行のプログラムを書け。  

(2) int型のビット長を整数で答えよ。

(3) 4つの整数型int, byte, long, shortをビット長が小さい順に並べよ。

(4) int型とuint型の違いを簡単に説明せよ。

(5) 実数(小数)を扱うことの出来る数値型を二つ答えよ。

(6) float型の0.08をリテラルを用いて表せ。

(7) char型の文字リテラルを何でも一つ示せ。

(8) bool型の値を全て示せ。

(9) 以下のプログラムの変数宣言の型を指定している部分はあるアルファベット3文字のキーワードに置き換えることが出来る。そのキーワードを答えよ。

    int num = 100;
    string str = "hello";
    Random random = new Random();

(10) 以下のプログラムがコンパイルエラーにならないように3.14をint型にキャストするプログラムを書け。

    int num = 2.5;

(11) (10)のプログラムを書き換えた時、numには結果的に何が代入されるか。数値で答えよ。

(12) 以下のプログラムの出力結果を答えよ。

    Console.WriteLine('F' - 'C');

(13) 以下のプログラムでエラーになる行をコメントに記述している番号で全て選べ。

    class Program
    {
        static void Main()
        {
            int a = 10; //①
            if(a >= 10)
            {
                b = b / 3; //②
                int b = a * a; //③
                if(b < 1000)
                {
                    int c = b + a; //④
                }
                int d = c * 2; //⑤
            }
            Console.WriteLine(a + b); //⑥
        }
    }

(14) Console.WriteLine(3333333333333333333 * 1000);とし画面に表示させたところ、-5527344008095509496と本来の計算結果とは明らかに異なる値が表示された。
このように値が大きすぎて計算結果がおかしくなることをなんというか。カタカナで答えよ。
(実際にはこのプログラムはuncheckedブロック内でないとビルドに失敗するが、今回は気にしなくて良い。)


Q3. **式と演算子**
---

(1) 余りを求める算術演算子を記号で答えよ。

(2) 数値型の変数から1を引く演算子を記号で答えよ。

(3) 以下のプログラムは画面にどのような値を出力するか。数値で答えよ。

    int a = 25;
    a += 5;
    Console.WriteLine(a);


以下の(4), (5), (6)では二つのint型の変数a, b, c, dがあるとする。例題と同じ形式で問いに答えよ。
例題: aとbが等しいかどうかを表す比較演算の式を答えよ
解答例: a == b

(4) aがb以下かどうかを表す比較演算の式を答えよ

(5) aがbと等しくないかどうかを表す比較演算の式を答えよ。

(6) aとbが等しい且つcがd未満かどうかを表す比較演算の式を答えよ。

(7) 以下の比較演算の式はtrueになるかfalseになるか。どちらかで答えよ。

    !((true && (true && false)) || (true || false))

(8) 以下のプログラムの出力結果を数値で答えよ。

    Console.WriteLine((((1 << 3) - 1) & 14) | 16);


Q4. **制御文**
---

以下の(1)ではint型の変数a, bがあるとする。

(1) aとbが等しいとき「おはよう」と表示し、aとbが等しくなく、aがb以上の時は「こんにちは」と表示し、それ以外の時は「こんばんは」と表示するプログラムを書け(Mainメソッドの内部のみでも良い)。

(2) for文を使って無限ループになるプログラムを書け。

(3) 以下のwhileループとおおよそ同じ動作をするforループを書け。

    int i = 0;
    while (i < 10)
    {
        Console.WriteLine(i);
        i++;
    }

(4) whileまたはforのループの中から途中で抜けるために使用する「b」から始まるアルファベット5文字のキーワードを書け。

(5) whileまたはforのループの途中で、そこから先の処理は飛ばして次のループに行く「c」から始まるアルファベット8文字のキーワードを書け。


Q5. **配列**
---

(1) 10個の要素を持つint型で配列名arrの配列を宣言せよ。

(2) arrという名前の配列がある時、arrの要素数(大きさ)を取得するにはどうすればよいか。arr.\*\*\*\*\*\*(\*は伏せ字)の形で書け。

(3) 5 * 10個の要素を持つint型で配列名arrの二次元配列を宣言せよ。

(4) 二次元配列の二次元目の要素数を取得するにはどうすればよいか。(2)と同じ形式で書け。

(5) 配列の添字はいくつから始まるか。数値で答えよ。

(6) 配列名arrのint型の配列がある。配列の全ての要素に10をかけるプログラムを書け。

(7) 配列名arrの配列がある(型はintとは限らない)。foreach文を用いて配列の全ての要素を画面に表示するプログラムを書け。

(8) 配列を小さい順に並び替えるメソッドの名前を答えよ。ヒント：Array.\*\*\*\*

(9) 配列の要素が0, 1, 2, 3, 4となるように配列の宣言と同時に初期化するプログラムを書け。配列名はarr、型はintとする。



Q6. **クラスとメソッド**
---

(1) メソッドから抜けるもしくは値を返す時に使用する「r」から始まるアルファベット6文字のキーワードを答えよ。

以下の(2)から(5)及び(9)と(10)で記述するメソッド、プロパティは静的メソッドで合ってもなくても良い。

(2) int型を一つ引数に取り、それに10を足してint型で返すメソッドを書け。

(3) int型を引数に取り、それに10を足したものを画面に表示し、何も値を返さずにメソッドから抜けるメソッドを書け。

(4) int型の引数 a を取り、aの階乗 (a! = a * (a - 1) * (a - 2) * (a - 3) * .... * 3 * 2 * 1)を再起を使って求めるメソッドを書け。

(5) 引数の値の正負を反転して(-1をかけて)返すメソッドを書け。ただし、メソッドのオーバーロードを用いてint型でもdouble型でも動作するようにすること。

(6) クラスを記述し、静的メンバ変数を宣言するプログラムを書け。クラス名はChara、静的メンバ変数名の変数名はspeedで型はintとする。

(7) クラスのメンバにクラスの外からアクセス出来るようにするにはどのようなアクセス修飾子をつければ良いか。「p」から始まるアルファベット6文字のキーワードを答えよ。

(8) アクセス修飾子を省略した場合、デフォルトではどのアクセス修飾子が付けられた場合と同様になるか。「p」から始まるアルファベット7文字のキーワードを答えよ。

(9) 値が0未満にならないint型のプロパティを書け。プロパティ名はCountとする。

(10) クラスの外部から値を取得することはできるが、値を変更することは出来ないint型のプロパティを作れ。プロパティ名はScoreとする。

(11) Charaという名前のクラスがあるとする。このクラスのコンストラクタを書け。

(12) 以下Charaクラスを継承したEnemyクラスを作成し、Walkメソッドをオーバーライドして x -= steps; とするプログラムを書け。

    class Chara
    {
        public int x = 0;
        
        public virtual void Walk(int steps)
        {
            x += steps;
        }
    }


Q7. **インターフェイス**
---

Walkメソッドを実装するインターフェイスを書け。Walkメソッドの返り値はint、引数無しとし、インターフェイスの名前はIWalkableとする。


Q8. **構造体**
---

クラスと構造体は似ているようで違いがいくつかあるが、その中の一つを簡潔に説明せよ。


Q9. **デリゲートとイベント**
---

以下の様にデリゲートとイベントが定義されているとする。

delegate int NumHandler(int a, int b);
NumHandler sumHandler;
event NumHanlder SumEvent;

int Sum(int a, int b)
{
    return a + b;
}

(1) SumEventにSumメソッドを追加するプログラムを書け。

(2) sumHandlerにラムダ式を用いてSumと同等の処理(デリゲートインスタンス)を代入するプログラムを書け。ただし内部でSumメソッドを使ってはいけない。


Q10. **例外**
---

(1) 以下のプログラムを実行し、数値以外の文字列を標準入力に入力したところ、FormatExceptionが発生しプログラムが異常終了した。
例外処理を用いてint.Parseに失敗した場合は「失敗」と出力し、プログラムが異常終了しないように書き直せ。

    string input = Console.ReadLine();
    int num = int.Parse(input) + 10;

(2) 以下のメソッドの冒頭に1行～数行加え、引数がnullだった場合はArugumentNullExceptionを発生させるようにせよ。

    int Sum(int[] arr)
    {
        //ここに書き加えるプログラムを書く
        return arr.Sum();
    }


Q11. **ジェネリック**
---

(1) ジェネリックを用いて以下のように使用できるPairクラスを作成せよ。

    Pair<int, string> pair = new Pair<int, string>();
    pair.First = 10;
    pair.Second = "hello";

(2) (1)で作成したクラスの型パラメータにはIDisposableインターフェイスを実装していなければ渡すことが出来ないという制約を設けたい。
class宣言の必要なところを書き換えよ。


Q12 **名前空間**
---

名前空間を定義する「n」から始まるアルファベット9文字のキーワードを答えよ。


Q13. **Windowsフォーム・ゲームプログラミング・その他**
---

(1) メッセージボックスを表示するプログラムを書け。

(2) フォームが読み込まれる時に発生するイベントの名前を答えよ。

(3) WindowsFormで画像を保持するのに用いられるクラスの名前を一つ答えよ。

(4) 画像を表示させられるWindowsFormのコントロールを一つ答えよ。

(5) WindowsFormのイベントで、キーが押された時に発生するイベントを一つ答えよ。

(6) 二つのオブジェクト(キャラクター)の当たり判定を行うHitメソッドを作りたい。コメントの仕様に沿ってメソッドの中身を書け。

    // p1: 一つめのオブジェクトの座標
    // s1: 一つめのオブジェクトの大きさ
    // p2: 二つめのオブジェクトの座標
    // p3: 二つめのオブジェクトの大きさ
    // 返り値: 当たった時は ture 当たっていない時は falseを返す。
    bool Hit(Point p1, Size s1, Point p2, Size s2)
    {
        //ここの中身を書く
    }

(7) Listクラスと配列の違いを一つ挙げよ。(「違うクラスである」のような解答は禁止)

fin.